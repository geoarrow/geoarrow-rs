APIs for writing GeoParquet data from Arrow [`RecordBatch`es][arrow_array::RecordBatch].

Both synchronous and asynchronous APIs are provided, with the latter being enabled by the `async` feature flag.
